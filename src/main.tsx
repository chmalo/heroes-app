import React from "react"
import ReactDOM from "react-dom/client"
import { HeroesApp } from "./HeroesApp"
import "animate.css"

ReactDOM.createRoot(document.getElementById("root")!).render(<HeroesApp />)
